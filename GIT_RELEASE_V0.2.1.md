# Release v0.2.1

## Summary

- Fixed Register screen overflow on smaller devices and when the keyboard is open.
- Polished education multi-select UI on the profile create/edit screen.
- Cleaned safe Flutter analyzer warnings without changing backend API contracts.
- Verified debug APK build with `flutter build apk --debug`.

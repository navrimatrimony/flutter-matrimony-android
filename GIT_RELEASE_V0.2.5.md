# Release v0.2.5

## Summary

- Added backend-backed `nearby` section support for More Matches.
- Added section-specific More Matches layouts instead of one shared grid for every section.
- Added submenu filter icon chip beside More Matches while keeping the appbar filter icon.
- Improved Recent Visitors empty-state clarity when the backend has no eligible rows to show.
- Kept More Matches backed by real backend data only; no fake backend data was added.

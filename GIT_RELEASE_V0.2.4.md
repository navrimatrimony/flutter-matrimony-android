# Release v0.2.4

## Summary

- Theme color changed to `#dc2626`.
- More Matches now uses gender-aware backend sections when available.
- Real backend-backed More Matches sections support added.
- Recent Visitors backend teaser/blur card rendering added.
- Appbar notification and filter icons added.
- Permanent filter row removed and filter panel compacted behind the appbar icon.
- Connect no longer uses a bottom sheet.
- Connect now uses a top horizontal submenu for existing interest screens.
- Duplicate Interest Sent text fixed on match cards.
- CTA strip compacted and aligned to the red brand color family.
- Interest CTA compact polish added for big match cards.
- Bottom icon strip added on Matches screen.
- More Matches preserves backend section order and falls back safely when unavailable.
- No fake chat/payment/contact logic was added.
- No fake backend logic was added.
